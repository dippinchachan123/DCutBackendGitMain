

export class Logs {
    // static getDescription(data : any) {
    //     switch(data.operation ) {
    //         case "Added":
    //             return 
    
    //         case "Deleted" :
    //             return `A new ${data.dataType} added of Weight /${data.weight}/.`

    //         case "Updated" :
    //             return `A new ${data.dataType} added of Weight /${data.weight}/.`

    //         case "Weight-Transfered" :
    //             return `A new ${data.dataType} added of Weight /${data.weight}/.`

    //         case "Issued" :
    //             return `A new ${data.dataType} added of Weight /${data.weight}/.`

    //         case "Returned" :
    //             return `A new ${data.dataType} added of Weight /${data.weight}/.`

    //         case "Un-Issued" :
    //             return `A new ${data.dataType} added of Weight /${data.weight}/.`

    //         case "Un-Returned" :
    //             return `A new ${data.dataType} added of Weight /${data.weight}/.`

    //         case "Un-Locked" :
    //     }
    // }
}